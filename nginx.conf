events {}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        listen 80;
        server_name 34.136.178.145;

        root /var/www/angular-app;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
